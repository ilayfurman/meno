// Deliberately empty: Face ID / biometric re-lock has been removed from the
// app entirely (see BiometricLockGate.tsx and ClerkAuthGate.tsx). Kept as
// an empty module rather than deleted because this project's mounted
// filesystem doesn't allow removing files from here.
export {};
